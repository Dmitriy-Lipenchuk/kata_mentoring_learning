Final могут быть:

1) Методы 
final метод не может быть переопределён в подклассах

2) Переменные 
Если переменная файнал, то в случае с примитивами значение таких переменных изменить нельзя, а
в с лучае с ссылочными переменными нельзя поменять ссыллку на объект, но при этом сам объект может
изменяться.

Так же final переменная может выступать в качестве аргумента метода
В таком случае это означает, что переменную нельзя менять внутри метода
Если же попытаться это сделать, то компилятор выдаст ошибку

3) Классы
От файнал классов нельзя наследоваться
