PULL ПРИМИТИВОВ

pull примитивов представляет из себя заранее записанные константы.
Для каждого класса-обёртки они свои (за исключением Double и Float, т.к у них нет пула примитивов)

Boolean: true false
Short，Integer，Long, Byte: от -128 до 127
Character: от 0 до 127

Верхний порог пула можно устанвалливать самостоятельно с помошью {@code -XX:AutoBoxCacheMax=<size>}
Пул создаётся при первом использовании класса-обёртки.

Если при инициализации класса-обёртки значение попадает в вышеописанный пул, то класс-обёртка
будет проинициализирован  значением из этого пула. Это означет, что две разные переменные, проинициализрованные
таким образом, имеют одинаковую ссылку. Можно избежать этого эффекта если воспользоваться инициализацией через
конструктор, но эти конструкторы помечены как depricated.

Пул примитивов хранится в heap, т.к его инициализация происходит в статик классе в статик блоке;
______________________________________________________________________________________________________________

PULL СТРОК

Pull строк представляет из себя место в хипе, которое хранит строки. При инициализации строки через литерал
джава сначала ищет такую же строку в пуле и если находит, то возвращает на неё ссылку, а если нет, то создаёт
новую строку в пуле и возвращает на неё ссылку. Такой подход возможен, т.к строки иммутабельны.

Строки, созданные через оператор new помещаются не в пул строк, а в хип. Их принудельно можно поместить в пул
методом intern();
