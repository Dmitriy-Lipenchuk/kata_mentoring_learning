raw type - ипользование дженерализированного класса без указания типа
Эта возможность оставлена в джава для обратной совместимости

Использование raw type приводит к ошибкам. Самое неприятное, что это ошибки в рантайме.

Рассмотрим предложенный пример.

В данном случае вызовется второй метод, т.к класс Gen не был параметризирован.
Если так сделать, то джава "забудет" о том, что у нас вообще есть какие-то параметры 
и методы после компиляции будут выглядеть вот так: 

void m (Collection collection);
void m (List list);

Вызовется второй метод, т.к List ближе к List чем к Collection, 
но при этом вылетит ClassCastException. Почему? Потому что во втором методе было явно указано, 
что мы перебираем массив стрингов и джава попытается преобразовать инт к стрингу.

Если же параметризировать класс Gen любым классом, то вызовется первый метод и программа отработает корректно, 
т.к джава не "забудет" о типах. Т.к List<Integer> и List<String> для джава абсолютно разные вещи, то она
вызовет метод в котором указаны коллекции. 
