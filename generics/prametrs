Параметризовать можно:
1) Класс верхнего уровня
2) Поле 
3) Метод (статические и не статические)
4) Интерфейс
5) Вложенные классы (статические и не статические)

Тестирующий код:

public class KataClass<T> {
    T value;

    static <T> void staticMethod(T obj) {
        //в данном случае T необходимо задать при вызове метода
        //в данном случае параметр "цепанётся" из параметра метода
    }

    T method(T obj) {
        return value;
    }

    class InnerClass<T> {
        //важно, что этот T отличен от T класса верхнего уровня, т.к перекрывает его,
        // но при этом во вложенном НЕ СТАТИЧЕСКОМ классе можно пользоваться типами класса верхнего уровня если нет перекрытия
        T value;
    }

    static class StatiicInnerClass<T> {
        //важно, что этот T отличен от T
        T value;
    }
    
}

interface RandomnInterface<T> {
    T someMethod();
}
