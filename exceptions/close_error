В таком случае вылетит ошибка IOException, что приведёт к экстренному завершеню программы 
Ресур не будет закрыт если ошибка вылетает при попытке закрыть ресурс.
Если в try возникла ошибка, то ошибка из close() будет подавлена (Подавленное исключение - возникшее исключение, 
но по какой-то причине подавленное другой. Самый частый сценарий подавленное исключение из close() в try-with-resources)
Исключения срабатывают в рантайме.

Тестирующий код: 

public class KataClass {

    public static void main(String[] args) throws IOException {
        try(InnerClass innerClass = new InnerClass()){
            //some logic
        }
    }

    static class InnerClass implements AutoCloseable{

        @Override
        public void close() throws IOException {
            throw new IOException();
        }
    }
}
