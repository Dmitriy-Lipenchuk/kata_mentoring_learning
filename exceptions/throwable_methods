1) addSuppressed(Throwable exception) - добавляет подавленное исключение

2) getSuppressed() - возвращает массив всех подавленных исключений 

3) getLocalizedMessage() - возращает локализованное собщщение об ошике. Если этот метод не переопределить, то 
по дефолту он будет возвращать результат getMessage()

4) getMessage() - вовзращает сообщение с деталями исключения

5) getCause() - возвращает причину этого throwable или null, если причина не существует или неизвестна

6) initCause(Throwable cause) - устаналивает причину (cause) у указанного исключения. Метод можно вызвать не более одного раза

7) getStackTrace() - возвращщает стэк-трейк в виде массива StackTraceElement. 

8) fillInStackTrace() - записывает информацию о текущем состоянии стек трейса

9) printStackTrace() - выводит стэк-трейс в стандарный поток вывода ошибок (значение переменной System.err)
   printStackTrace(PrintStream s) - аналогично методу выше, но поток вывода задаётся c помошью PrintStream (вывод в консоль)
   printStackTrace(PrintWriter s) - аналогично методу выше, но поток вывода задаётся c помошью PrintWriter (вывод в консоль, файл или любой другой поток)

10) setStackTrace(StackTraceElement[] stackTrace) - задаёт стэк-трейс
