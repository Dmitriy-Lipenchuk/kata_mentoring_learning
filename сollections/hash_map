Работа хэш мапы основана на хэшэ 
Все элементы хранятся в массиве элементами которго является LinkedList. В нодах листа хранятся:
ключ-значение, хэш, а так же ссылка на следующий элемент.

Ключ может быть NULL.
Если ключ NULL то объект кладётся в первый бакет!

При добавлении элемента (метод put) высчитывается хэш ключа, а после берётся остаток от деления на текущий размер
массива. Полученное в результате деления число является индексом элемента в массиве. После элемент добавляется в бакет.
Если в бакете уже есть объекты, то происходит сравнение хэшей. Если сравнение false, то объект просто добавляется в текущий
бакет, а если true, то происходит сравнение ключей через equals и если сравнение true, то объект перезаписывается. 

Если количетво бакетов 64 и если в бакете 8 или больше элементов, то при добавлении нового элемента
LinkedList перестраивается в кч дерево
Для обратного перехода в бакете должно быть 6 или меньше элементов

Метод get() работает похожим на put() образом. Высчитывает хэш -> вычисляет нужный элемент в массиве -> сравнивает
хэши -> если равны, то сравнивает ключи через equalыs -> возвращает значение. Если такого элемента нет, то возаращает
null.
