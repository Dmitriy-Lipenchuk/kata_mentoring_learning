В ArrayList и LinkedList при вызове contains произойдёт перебор всех элементов и во время перебора найдётся хоть 
один подходяший, то метод вернёт true (иначе false)

В HashSet поиск будет быстрее, т.к для поиска достаточно посчитать хэш и посмотреть лежит ли проверяемое значение в 
сооветсвующем индексе массива.
