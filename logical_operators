1) & (логическое и)
2) && (сокращщёное и)
3) | (логическое или)
4) || (сокращенное или)
5) ^ (XOR исключающее или)
6) ! (не)
7) == (равно)
8) != (не равно)
9) ?: (тернарник)


A      B      A|B    A&B    A^B
false  false  false  false  false
true   false  true   false  true
false  true   true   false  true
true   true   true   true   false

Сокращенное и аналогично по таблицы истинности обычному и, но логика работы несколько отличается.
Сокращенное и если видит, что первый проверяемый элемент равен false, то сразу же возвращает false не проверяя второй.
С сокращенным или похожая история, но только он возвраает true если видит и первого проверяемого элемента true;
