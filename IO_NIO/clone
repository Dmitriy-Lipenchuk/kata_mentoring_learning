При поверхностном создаётся новый объект, но если у клонируемого объекта есть ссылочные переменные, то они не копируются,
а берётся ссылка на них и назначается клону, что может привести к ошибкам.
При глубоком копировании создаётся новый объект, идентичный клонируемому. Ссылочные поля так же копируются (ссылки не равны)

Глубокое клонирование можно реализовать 3 способами: 
1) С помошью clone() 

2) C помошью конструктора 

3) С помошью сереализации
